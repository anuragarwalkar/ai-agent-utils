// Simple test to verify the build setup
import React from 'react';

const TestApp = () => {
  return React.createElement('div', null, 'Test App Working');
};

console.log('Test file loaded successfully');
export default TestApp;
